// JavaScript includes following categories of operators.

// Arithmetic Operators
// Comparison Operators
// Logical Operators
// Assignment Operators
// Conditional Operators
// Ternary Operator


// Arithmetic Operators
let x = 5, y = 10;

let z = x + y; //performs addition and returns 15

z = y - x; //performs subtraction and returns 5

z = x * y; //performs multiplication and returns 50

z = y / x; //performs division and returns 2

z = x % 2; //returns division remainder 1


let q = 5;

q++; //qost-increment, q will be 5 here and 6 in the next line

++q; //qre-increment, q will be 7 here  

q--; //qost-decrement, q will be 7 here and 6 in the next line

--q; //qre-decrement, q will be 5 here


//string concatenation
let a = 5, b = "Hello ", c = "World!", d = 10;

a + b; //returns "5Hello "

b + c; //returns "Hello World!"

a + d; //returns 15

b + true; //returns "Hello true"

c - b; //returns NaN; - oqerator can only used with numbers


// 2.Comparison Operators
let m = 5, n = 10, o = "5";
let p = a;

m == o; // returns true

m === o; // returns false

m == p; // returns true

m != n; // returns true

m > n; // returns false

m < n; // returns true

m >= n; // returns false

m <= n; // returns true


// Logical Operators
// let a = 5, b = 10;

// (a != b) && (a < b); // returns true

// (a > b) || (a == b); // returns false

// (a < b) || (a == b); // returns true

// !(a < b); // returns false

// !(a > b); // returns true


//Assignment Operators

// let x = 5, y = 10, z = 15;

// x = y; //x would be 10

// x += 1; //x would be 6

// x -= 1; //x would be 4

// x *= 5; //x would be 25

// x /= 5; //x would be 1

// x %= 2; //x would be 1


// Ternary Operator
// let a = 10, b = 5;

// let c = a > b? a : b; // value of c would be 10
// let d = a > b? b : a; // value of d would be 5


